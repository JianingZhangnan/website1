# Claude Code entrypoint

Read `AGENTS.md`, then `docs/AI_HANDOFF.md` and `docs/DESIGN_BRIEF.md`, before making changes.

`AGENTS.md` is the canonical instruction file. Keep this file short so the two instruction sets cannot drift.
