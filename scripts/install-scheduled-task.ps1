[CmdletBinding()]
param()

throw @'
Automatic FPKS publication has been retired.

The private FPKS vault is synchronized only by Baidu Netdisk. Website snapshots now require an explicit manual preview and apply step; this repository must not reinstall the old scheduled publisher.
'@
